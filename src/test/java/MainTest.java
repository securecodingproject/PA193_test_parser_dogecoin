import static org.junit.Assert.assertTrue;
import org.junit.Test;
import parser.Main;

import java.nio.ByteBuffer;

public class MainTest {
    @Test
    public void dummy() { assertTrue(Main.dummy()); }
}